run = "npm run dev -- --host 0.0.0.0 --port 3000"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "npm run build && npx serve dist -s -l 3000"]
build = ["sh", "-c", "npm install"]

[[ports]]
localPort = 3000
externalPort = 80
